//! Hark dev binary: load config -> resolve key -> run the pipeline -> block
//! on Ctrl+C. Populated in Phase 1 checkpoint 6. Logging must never emit key
//! material or raw audio (lengths/counts only).

fn main() {}
