# /dev/uinput does not exist until the uinput module is loaded, and nothing on
# a desktop system loads it on its own. Hark synthesizes its paste keystroke
# through a uinput virtual keyboard on Wayland, so the module has to be there
# before the user's session starts -- loading it lazily at first paste would
# need root.
uinput
